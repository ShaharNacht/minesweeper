// Always include this file instead of including SDL directly, to make sure that SDL_MAIN_HANDLED is always defined.

#pragma once

#define SDL_MAIN_HANDLED
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>